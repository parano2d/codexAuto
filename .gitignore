.DS_Store
Thumbs.db
node_modules/
.env
.env.*
*.log

# Keep local runtime snapshots out of commits unless explicitly needed.
.codex-handoff/latest-snapshot.json

# codexAuto is the global handoff/manual repository.
# Project source files should live in their own project repositories unless explicitly added.
/index.html
/logo_black.svg
/logo_white.svg

# Do not commit the full Codex home. Only selected session exports under /codex-sessions are tracked.
/.codex/
